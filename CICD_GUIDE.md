# CI/CD 파이프라인 가이드

이 문서는 i18nexus monorepo의 CI/CD 파이프라인을 이해하기 쉽게 설명합니다.

## 📋 목차

1. [전체 흐름 개요](#전체-흐름-개요)
2. [워크플로우별 상세 설명](#워크플로우별-상세-설명)
3. [실제 사용 예시](#실제-사용-예시)
4. [문제 해결](#문제-해결)

---

## 전체 흐름 개요

### 🔄 개발 → 배포 전체 흐름

```
개발자가 코드 작성
    ↓
git commit (커밋 컨벤션 검증)
    ↓
git push origin main
    ↓
┌─────────────────────────────────────┐
│  GitHub Actions 트리거              │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  1. CI 워크플로우 (항상 실행)       │
│     - 변경된 패키지 감지             │
│     - 린트, 빌드, 테스트            │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  2. Version and Release 워크플로우  │
│     - 릴리즈 커밋 감지?              │
│       YES → 자동 버전 업데이트 & 배포│
│       NO  → 스킵                    │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  3. Deploy 워크플로우 (레거시)      │
│     - 수동 배포용 (사용 안 함)       │
└─────────────────────────────────────┘
```

### 📦 패키지별 배포 흐름

```
feat[core]: version 3.0.0 release
    ↓
[Version and Release 워크플로우]
    ↓
1. 커밋 메시지 파싱 → 버전 3.0.0 추출
2. packages/core/package.json → version: "3.0.0" 업데이트
3. Turbo로 빌드 (캐시 활용)
4. NPM에 i18nexus@3.0.0 배포
5. Git 태그 core-v3.0.0 생성
6. 버전 업데이트 커밋 푸시
```

---

## 워크플로우별 상세 설명

### 1️⃣ CI 워크플로우 (`.github/workflows/ci.yml`)

**목적**: 모든 커밋에 대해 코드 품질 검증

**트리거**:
- `push` to `main` 브랜치
- `pull_request` to `main` 브랜치

**실행 단계**:

#### Job 1: `detect-changes` (변경 감지)

```yaml
목적: 어떤 패키지가 변경되었는지 감지
```

**동작 방식**:
1. `dorny/paths-filter` 액션으로 변경된 파일 경로 분석
2. 각 패키지별로 변경 여부 판단:
   - `core`: `packages/core/**` 변경 시
   - `tools`: `packages/tools/**` 변경 시
   - `demo`: `apps/demo/**` 또는 의존성 패키지 변경 시

**출력값**:
- `core`: `true` 또는 `false`
- `tools`: `true` 또는 `false`
- `demo`: `true` 또는 `false`

**예시**:
```bash
# packages/core/src/index.ts 수정 시
core=true, tools=false, demo=false

# apps/demo/app/page.tsx 수정 시
core=false, tools=false, demo=true

# packages/core와 apps/demo 모두 수정 시
core=true, tools=false, demo=true
```

#### Job 2: `build-and-test` (빌드 및 테스트)

```yaml
목적: 변경된 패키지만 선택적으로 빌드/테스트
```

**실행 순서**:

1. **의존성 설치**
   ```bash
   npm ci  # package-lock.json 기반 정확한 설치
   ```

2. **Core 패키지 우선 빌드** (의존성 때문)
   ```bash
   # core 또는 tools 변경 시에만 실행
   npx turbo run build --filter=i18nexus
   npx turbo run build --filter=i18nexus-tools
   ```

3. **린트 실행** (필수, 실패 시 중단)
   ```bash
   # 변경된 패키지만 린트
   npx turbo run lint --filter=i18nexus
   ```

4. **빌드 실행** (Turbo 캐시 활용)
   ```bash
   # Turbo 원격 캐시 사용
   TURBO_TOKEN=${{ secrets.TURBO_TOKEN }}
   npx turbo run build --filter=i18nexus
   ```
   - **캐시 히트**: 이전 빌드와 동일하면 스킵 (FULL TURBO 🔥)
   - **캐시 미스**: 새로 빌드

5. **테스트 실행** (선택적, 실패해도 계속)
   ```bash
   npx turbo run test --filter=i18nexus
   ```

**성능 최적화**:
- ✅ 변경된 패키지만 처리 (Incremental Build)
- ✅ Turbo 원격 캐시로 빌드 시간 단축
- ✅ 병렬 실행 (Turbo 내부)

---

### 2️⃣ Version and Release 워크플로우 (`.github/workflows/version-and-release.yml`)

**목적**: 특정 커밋 메시지 패턴 감지 시 자동 버전 업데이트 및 배포

**트리거**:
- `push` to `main` 브랜치 (모든 커밋)

**핵심 기능**: 릴리즈 커밋 감지 및 자동 배포

#### Job 1: `check-release-commit` (릴리즈 커밋 감지)

```yaml
목적: 커밋 메시지에서 릴리즈 패턴 찾기
```

**감지 패턴**:
```bash
# Core 패키지 릴리즈
feat[core]: version 3.0.0 release

# Tools 패키지 릴리즈
feat[tools]: version 1.5.0 release

# Demo 앱 릴리즈
feat[demo]: version 3.4.0 release
```

**동작 방식**:
1. 최신 커밋 메시지 읽기
2. 정규식으로 패턴 매칭:
   ```regex
   ^feat\[(core|tools|demo)\]:\s*version\s+[0-9]+\.[0-9]+\.[0-9]+\s+release
   ```
3. 버전 번호 추출 (예: `3.0.0`)
4. 출력값 설정:
   - `should_release_core`: `true`/`false`
   - `core_version`: `"3.0.0"` (감지된 경우)

**예시**:
```bash
# 커밋 메시지
feat[core]: version 3.0.0 release

# 출력
should_release_core=true
core_version=3.0.0
should_release_tools=false
should_release_demo=false
```

#### Job 2: `release-core` (Core 패키지 배포)

**조건**: `should_release_core == 'true'`일 때만 실행

**실행 단계**:

1. **코드 체크아웃**
   ```bash
   git checkout (with write permission)
   ```

2. **Node.js 설정**
   ```bash
   node-version: "20"
   registry-url: "https://registry.npmjs.org"
   ```

3. **의존성 설치**
   ```bash
   npm ci
   ```

4. **버전 업데이트** ⭐
   ```bash
   cd packages/core
   npm version 3.0.0 --no-git-tag-version --allow-same-version
   # package.json의 version 필드가 "3.0.0"으로 변경됨
   ```

5. **Turbo로 빌드** (캐시 활용)
   ```bash
   TURBO_TOKEN=${{ secrets.TURBO_TOKEN }}
   npx turbo run build --filter=i18nexus
   ```

6. **NPM 버전 확인**
   ```bash
   npm view i18nexus@3.0.0 version
   # 이미 존재하면 배포 스킵
   ```

7. **NPM 배포** (버전이 새 것일 때만)
   ```bash
   cd packages/core
   npm publish --access public
   # → https://www.npmjs.com/package/i18nexus
   ```

8. **버전 업데이트 커밋**
   ```bash
   git add packages/core/package.json
   git commit -m "chore[core]: bump version to 3.0.0"
   git push origin main
   ```

9. **Git 태그 생성**
   ```bash
   git tag -a "core-v3.0.0" -m "Release i18nexus@3.0.0"
   git push origin core-v3.0.0
   ```

**결과**:
- ✅ NPM에 `i18nexus@3.0.0` 배포됨
- ✅ Git 태그 `core-v3.0.0` 생성됨
- ✅ `package.json` 버전 업데이트 커밋 푸시됨

#### Job 3: `release-tools` (Tools 패키지 배포)

Core와 동일한 프로세스, `packages/tools` 대상

**태그 형식**: `tools-v1.5.0`

#### Job 4: `release-demo` (Demo 앱 버전 관리)

**차이점**:
- NPM 배포 없음 (앱이므로)
- Vercel이 자동 배포 (GitHub 연동)
- Git 태그만 생성: `demo-v3.4.0`

---

### 3️⃣ Deploy 워크플로우 (`.github/workflows/deploy.yml`)

**목적**: 레거시 워크플로우 (수동 배포용)

**현재 상태**: 
- ⚠️ **사용 안 함** (Version and Release가 자동 처리)
- 수동 배포가 필요할 때만 사용

**트리거**:
- `push` to `main` (특정 경로 변경 시)
- `workflow_dispatch` (수동 실행)

**차이점**:
- Version and Release: 커밋 메시지 기반 자동 배포
- Deploy: 현재 `package.json` 버전 그대로 배포

---

## 실제 사용 예시

### 시나리오 1: 일반 개발 커밋

```bash
# 1. 코드 수정
vim packages/core/src/utils/createI18n.ts

# 2. 커밋 (커밋 컨벤션 준수)
git add packages/core/
git commit -m "feat[core]: add lazy loading support"
git push origin main
```

**실행되는 워크플로우**:
1. ✅ **CI 워크플로우** 실행
   - `detect-changes`: `core=true` 감지
   - `build-and-test`: 린트, 빌드, 테스트 실행
2. ❌ **Version and Release**: 릴리즈 패턴 없음 → 스킵
3. ❌ **Deploy**: 경로 필터 미충족 → 스킵

**결과**: 코드 검증만 수행, 배포 없음

---

### 시나리오 2: Core 패키지 릴리즈

```bash
# 1. 릴리즈 준비 완료 후
git commit --allow-empty -m "feat[core]: version 3.0.0 release"
git push origin main
```

**실행되는 워크플로우**:
1. ✅ **CI 워크플로우** 실행 (일반 검증)
2. ✅ **Version and Release** 실행
   - `check-release-commit`: 패턴 감지 ✅
   - `release-core`: 자동 배포 시작
     - `package.json` → `3.0.0` 업데이트
     - 빌드 → NPM 배포 → 태그 생성

**결과**:
- NPM: `i18nexus@3.0.0` 배포됨
- Git: `core-v3.0.0` 태그 생성됨
- GitHub: 버전 업데이트 커밋 자동 생성

---

### 시나리오 3: 여러 패키지 동시 릴리즈

```bash
# 각각 별도 커밋으로
git commit --allow-empty -m "feat[core]: version 3.0.0 release"
git push origin main
# → Core 배포 완료 대기

git commit --allow-empty -m "feat[tools]: version 1.5.0 release"
git push origin main
# → Tools 배포 완료
```

**권장**: 한 번에 하나씩 릴리즈 (충돌 방지)

---

## 워크플로우 비교표

| 워크플로우 | 트리거 | 목적 | 자동화 수준 |
|-----------|--------|------|------------|
| **CI** | 모든 push/PR | 코드 검증 | ✅ 완전 자동 |
| **Version and Release** | 모든 push (패턴 감지) | 버전 관리 & 배포 | ✅ 완전 자동 |
| **Deploy** | 수동 또는 경로 기반 | 레거시 배포 | ⚠️ 수동 |

---

## 커밋 컨벤션과의 연계

### 커밋 메시지 형식

```bash
<type>[<scope>]: <subject>
```

**필수 Scope**:
- `core` - i18nexus 패키지
- `tools` - i18nexus-tools 패키지
- `demo` - demo 앱

**릴리즈 커밋 형식**:
```bash
feat[scope]: version X.X.X release
```

### Commitlint 검증

`.husky/commit-msg` hook이 자동으로 검증:
- ❌ `feat: add feature` → scope 누락
- ❌ `feat(core): add feature` → 대괄호 사용해야 함
- ✅ `feat[core]: add feature` → 통과

---

## Turbo 캐싱 활용

### 원격 캐싱 설정

**필요한 Secrets**:
- `TURBO_TOKEN`: Vercel에서 생성
- `TURBO_TEAM`: Vercel 팀 ID (선택)

**효과**:
```bash
# 첫 빌드 (캐시 없음)
npx turbo run build
# → 2분 소요

# 두 번째 빌드 (캐시 히트)
npx turbo run build
# → 5초 소요 (FULL TURBO 🔥)
```

**CI/CD에서의 활용**:
- 모든 빌드/테스트 단계에서 캐시 사용
- 변경되지 않은 패키지는 스킵
- 빌드 시간 대폭 단축

---

## 문제 해결

### 1. CI가 실패하는 경우

**원인**:
- 린트 에러
- 빌드 에러
- 테스트 실패

**해결**:
```bash
# 로컬에서 확인
npm run lint
npm run build
npm run test
```

### 2. 배포가 안 되는 경우

**확인 사항**:
1. 커밋 메시지 형식 확인
   ```bash
   # 올바른 형식
   feat[core]: version 3.0.0 release
   
   # 잘못된 형식
   feat[core]: version 3.0.0  # "release" 누락
   feat: version 3.0.0 release  # scope 누락
   ```

2. GitHub Secrets 확인
   - `NPM_TOKEN` 설정되어 있는지
   - 권한이 올바른지

3. NPM 버전 중복 확인
   ```bash
   npm view i18nexus@3.0.0
   # 이미 존재하면 배포 스킵됨
   ```

### 3. Turbo 캐시가 작동 안 하는 경우

**확인**:
```bash
# 로컬에서 테스트
export TURBO_TOKEN=your-token
npx turbo run build --summarize
```

**해결**:
- GitHub Secrets에 `TURBO_TOKEN` 추가
- Vercel에서 토큰 재생성

---

## 요약

### ✅ 자동화된 프로세스

1. **코드 품질 검증**: 모든 커밋에 대해 자동 실행
2. **버전 관리**: 릴리즈 커밋 시 자동 버전 업데이트
3. **NPM 배포**: 자동으로 NPM에 배포
4. **Git 태그**: 자동으로 버전 태그 생성

### 🎯 개발자 작업

1. 코드 작성
2. 커밋 (커밋 컨벤션 준수)
3. 푸시
4. **끝!** (나머지는 자동)

### 📦 릴리즈 작업

1. 릴리즈 커밋 생성
   ```bash
   git commit --allow-empty -m "feat[core]: version 3.0.0 release"
   ```
2. 푸시
   ```bash
   git push origin main
   ```
3. **끝!** (배포는 자동)

---

## 참고 자료

- [GitHub Actions 문서](https://docs.github.com/en/actions)
- [Turborepo 문서](https://turbo.build/repo/docs)
- [Commitlint 문서](https://commitlint.js.org/)
- [NPM 배포 가이드](https://docs.npmjs.com/packages-and-modules/contributing-packages-to-the-registry)

