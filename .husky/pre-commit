#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# CI 환경에서는 스킵
if [ -n "$CI" ]; then
  echo "⏭️  CI environment detected, skipping pre-commit hook"
  exit 0
fi

echo "🔍 Running linter on staged files..."

# lint-staged로 변경된 파일 포맷팅
npx lint-staged || {
  echo "⚠️  Lint-staged failed, but continuing..."
  echo "💡 Files can be formatted manually with: npm run format"
}

echo "✅ Pre-commit checks completed!"
