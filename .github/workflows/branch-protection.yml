name: Branch Protection

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-branch-protection:
    name: Check Branch Protection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check if PR is required
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "⚠️  Direct push to main branch detected"
          echo "💡 For better code quality, please use Pull Requests"
          echo "✅ This workflow allows direct push, but PR is recommended"

